# Wildfire Tracker 🔥
Get wildfire events using the NASA Earth Observatory Natural Event Tracker (EONET) API.

## Usage
### Start the Django App
Run this commmand from root directory: `python3 django/manage.py runserver`

OR 

### Run the Script
Run the main.py script from root directory: `python3 main.py <state> <year>`